{
  "name": "boid-backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "invoke": "serverless invoke local -f register",
    "deploy": "serverless deploy --stage dev --verbose",
    "remove": "serverless remove -s dev -r us-east-2"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dialogflow": "^1.2.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.1.0"
  }
}
